{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ornitzaileak</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ornitzaileak.id }}</td>
            </tr>
            <tr>
                <th>Izena</th>
                <td>{{ ornitzaileak.izena }}</td>
            </tr>
            <tr>
                <th>Helbidea</th>
                <td>{{ ornitzaileak.helbidea }}</td>
            </tr>
            <tr>
                <th>Oharrak</th>
                <td>{{ ornitzaileak.oharrak }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ornitzaileak_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('ornitzaileak_edit', { 'id': ornitzaileak.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
